import { useNavigate } from "react-router-dom";import { motion } from "framer-motion";function Login() { const navigate = useNavigate(); const handleSubmit = (e) => { e.preventDefault(); const name = e.target.name.value; const country = e.target.country.value; const age = e.target.age.value; if (age < 15) { alert("👻 You are too young for these horror movies!"); return; } localStorage.setItem("name", name); localStorage.setItem("country", country); localStorage.setItem("age", age); navigate("/preferences"); }; return ( <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}> <div className="container"> <h1>Enter the Haunted World</h1> <form onSubmit={handleSubmit}> <input name="name" type="text" placeholder="Name" required /> <input name="country" type="text" placeholder="Country" required /> <input name="age" type="number" placeholder="Age" required /> <button type="submit">Enter</button> </form> </div> </motion.div> );}export default Login;